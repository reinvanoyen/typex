{
  "name": "typex",
  "version": "1.0.0",
  "description": "Text styles to CSS, SASS mixins, HTML, JSON, ...",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reinvanoyen/typex"
  },
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "typex",
    "manifest": "src/manifest.json",
    "main": "typex.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.2"
  }
}
